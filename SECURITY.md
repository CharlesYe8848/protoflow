# 安全说明

ProtoFlow 是运行在个人电脑上的可信项目工具，不提供多租户隔离或不可信代码沙箱。

- 预览服务仅监听 `127.0.0.1`，校验 Host 和 Origin；不要将它反向代理或转发到公网。
- HTTP 文件入口限制文件类型，拒绝隐藏文件与符号链接。画布状态 JSON 和截图预览 HTML 是明确的隐藏目录例外。
- 项目 JSX 会在浏览器执行；`doc-kinds/*/checks/*.mjs` 会以当前用户权限在子进程执行。子进程超时处理不等于权限隔离。只使用可信项目和文档类型包。
- 截图使用无头 Chrome；当前截图进程带有 `--no-sandbox` 参数。不要用它截图不可信项目。
- 所有已注册项目共用一个本地预览服务及浏览器 origin，不适合同时打开互不信任的项目。
- 发布到钉钉等平台由调用方的连接器完成，权限和凭据由相应平台工具管理。

## 报告问题

如仓库已开启私密漏洞报告，请使用 [GitHub Security](https://github.com/CharlesYe8848/protoflow/security/advisories/new)。
若入口不可用，请仅开一个请求私下联系的 Issue，不要公开漏洞细节、凭据或真实业务数据。

当前维护版本为 `0.1.x` 预览版，优先在最新版本复现和修复问题。
